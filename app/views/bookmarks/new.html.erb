<div>
  <%= simple_form_for([@list, @bookmark]) do |f| %>
    <%= f.input :comment %>
    <%# <%= f.input :movie %>
    <%= f.submit %>
  <% end %>
</div>
<%= link_to "Back to the list", lists_path(@lists) %>
