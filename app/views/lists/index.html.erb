<h1> Yopity </h1>
<br>
<%# <%= cl_image_tag("THE_IMAGE_ID_FROM_LIBRARY", width: 400, height: 300, crop: :fill) %>
<br>
<ul>
  <% @lists.each do |list| %>
      <li><%= link_to list.name, list_path(list) %></li>
  <% end %>

</ul>
<br>
<%= link_to "New list ?", new_list_path(@new) %>
